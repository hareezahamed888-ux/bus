# JPR Bus Pass

College bus pass web app (Student / Alumni / Parent).

## Local Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## Vercel

This repo is set up as a Vite + React project.  
Pushing to `main` automatically triggers a Vercel deployment (project is already linked).
