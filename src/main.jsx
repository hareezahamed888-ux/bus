import React from 'react'
import ReactDOM from 'react-dom/client'
import JPRBusApp from '../JPRBusApp.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <JPRBusApp />
  </React.StrictMode>,
)
